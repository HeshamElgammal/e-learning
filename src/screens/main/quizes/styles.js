const { StyleSheet } = require("react-native");
const { appColors } = require("theme");

export const commonStyles=StyleSheet.create({
    container:{
        width:"100%",
        height:'100%',
        backgroundColor:appColors.white
    }
})